Initially you can run npm install command on terminal , extention like vs code, 

### `npm install`

then you can run npm start command ,

### `npm start`

once you run npm start command then it will run on browser http://localhost:3000